"""Bimanual sphere-reaching environments for humanoid robots.

The robot keeps its default pose while the two arms track a commanded pair of
3D target points (one per hand). Legs and torso are held by a posture reward
rather than by welding the pelvis, so the task still runs on the full robot.
"""
